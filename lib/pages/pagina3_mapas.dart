import 'package:flutter/material.dart';

class Pagina3Mapas extends StatelessWidget {
  const Pagina3Mapas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Pagina de mapas")),
    );
  }
}
