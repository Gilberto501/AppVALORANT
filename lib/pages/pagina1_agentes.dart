import 'package:flutter/material.dart';

class Pagina1Agentes extends StatelessWidget {
  const Pagina1Agentes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Pagina de Agentes")),
    );
  }
}
